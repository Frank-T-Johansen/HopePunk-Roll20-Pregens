# Changelog

## Best-of-both-worlds importer

- Kept current `Name (Background)` Journal naming for player-facing pregen selection.
- Kept v0.7 field coverage from the bundled data: `movement_speed`, `armour_N_trait_1..4`, and `cyberware_N_trait_1..4`.
- Kept optional handout import behavior.
- Added selected Roll20 graphic linking commands for avatars and default tokens.
- Added alias matching for filenames with or without `.png`, `portrait`, `token`, or `pregen` in the staged graphic name.
